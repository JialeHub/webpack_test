/* eslint-disable */
console.log('加载myMath模块...')
export default function mul(x, y) {
  return x * y;
}

export function add(x, y) {
  return x + y;
}

export function add2(x, y) {
  return 2 * (x + y);
}
